# Environment variables for running WordyMe with Docker Compose.
# Copy to .env and edit:  cp .env.example .env

# REQUIRED. There is deliberately no default: a default committed to a public
# repository is a published secret, and anyone running it unchanged could forge
# sessions. Compose refuses to start until this is set.
# Generate one with:  openssl rand -base64 32
BETTER_AUTH_SECRET=

# Comma-separated: every origin you use (Better Auth trusted origins + CORS).
# Example for LAN access:
# CLIENT_URL=http://localhost:8080,http://192.168.1.20:8080
CLIENT_URL=http://localhost:8080

# Optional. Public origin where the app is served (session cookies use http vs https from this). Defaults to first CLIENT_URL.
# Set when the canonical URL is not first in CLIENT_URL, e.g. BETTER_AUTH_URL=https://notes.example.com
# BETTER_AUTH_URL=

# Optional. Host port Compose publishes to. Change this if 8080 is already taken,
# and update CLIENT_URL to match.
# HOST_PORT=8080

# Optional. Set ONLY when a reverse proxy sits in front of the container, so the
# client address it reports is used for login rate limiting. Accepts a hop count
# (1), true, or a list of trusted addresses. Leave unset when the port is reached
# directly — trusting X-Forwarded-For from arbitrary clients lets them forge an
# address per request and evade rate limiting entirely.
# TRUST_PROXY=1

# Note: VITE_BACKEND_URL is not needed for Docker. The backend serves the web
# bundle from the same origin, so API calls are relative. It applies only if you
# host the frontend on a different origin from the API — and because Vite inlines
# it at build time, doing so pins the build to one hostname.
